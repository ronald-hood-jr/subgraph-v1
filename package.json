{
  "name": "v1",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ ichi-org/v1",
    "create-local": "graph create --node http://localhost:8020/ ichi-org/v1",
    "remove-local": "graph remove --node http://localhost:8020/ ichi-org/v1",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ichi-org/v1"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.27.0",
    "@graphprotocol/graph-ts": "0.25.0"
  }
}
